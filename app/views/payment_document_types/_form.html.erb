<%= simple_form_for @payment_document_type, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= error_span(@payment_document_type[:name]) %>
  <%= f.input :description %>
  <%= error_span(@payment_document_type[:description]) %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                payment_document_types_path, :class => 'btn btn-default' %>
<% end %>
