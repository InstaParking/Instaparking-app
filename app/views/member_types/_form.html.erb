<%= simple_form_for @member_type, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= error_span(@member_type[:name]) %>
  <%= f.input :description %>
  <%= error_span(@member_type[:description]) %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                member_types_path, :class => 'btn btn-default' %>
<% end %>
