<h1>Opiniones de nuestros Usuarios</h1>

<!--
<div class="">
    <ul class="list-group">
        
        <% @feedbacks.each do |feed| %>
        <li class="list-group-item">
            <h4 class="list-group-item-heading"><%= feed.booking.user.profile.first_name %> </h4>
            <p><%= feed.comment %></p>
            <span class="badge text-xs"><%= feed.score %></span>  Puntaje
        </li>
        <% end %>
    </ul>
</div>
-->

<% @feedbacks.each do |feed| %>
<div class="panel panel-default">
  <!-- Default panel contents -->
    <div class="panel-heading"><%= feed.booking.user.name %><br/>
        Puntaje: <span class="badge text-sm"><%= feed.score %></span>
    </div>
  <div class="panel-body">
    <p><%= feed.comment %></p>
  </div>

</div>
<% end %>