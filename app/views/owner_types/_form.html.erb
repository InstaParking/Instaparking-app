<%= simple_form_for @owner_type, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= error_span(@owner_type[:name]) %>
  <%= f.input :description %>
  <%= error_span(@owner_type[:description]) %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                owner_types_path, :class => 'btn btn-default' %>
<% end %>
